# Demo: Clasificador de emociones (TensorFlow/Keras)

Este ejemplo muestra el flujo completo de un modelo NLP sencillo:
tokenización → padding → embedding → entrenamiento → predicción.

El dataset es pequeño y solo se usa con fines educativos.
No representa un modelo válido para producción.

## Cómo ejecutar
1. Instalar dependencias:
```bash
pip install -r requirements.txt

